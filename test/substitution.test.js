// Write your tests here!
const { substituion } = require("../src/substitution.js");
const { expect } = require("chai").expect;

describe("substitution()", () => {
    it("Should return false if the substitution alphabet is missing", () => {
        
    })
})